FROM node:16
WORKDIR /app

COPY ./ui/dashboard .
RUN npm install
EXPOSE 5173

ARG PUBLIC_IP
ENV PUBLIC_IP=$PUBLIC_IP
ENV FEDERATION_SERVER="http://$PUBLIC_IP:5433"
ENV AUTH_SERVER="http://$PUBLIC_IP:8463"

CMD [ "npm","run","dev" ] 